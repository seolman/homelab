apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-tls
  namespace: monitoring
spec:
  secretName: grafana-tls
  issuerRef:
    name: cloudflare-issuer
    kind: ClusterIssuer
  commonName: grafana.seolman.dev
  dnsNames:
    - grafana.seolman.dev
