apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: infra-set
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - git:
      repoURL: https://github.com/seolman/homelab.git
      revision: HEAD
      directories:
      - path: infra/overlays/prod/*
  template:
    metadata:
      name: "{{.path.basename}}-app"
      namespace: argocd
    spec:
      project: default
      source:
        repoURL: https://github.com/seolman/homelab.git
        targetRevision: HEAD
        path: "{{.path.path}}"
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{.path.basename}}-system"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
